# A11 刷新停滞诊断（2026-09-21）

产品修复保留。网站恢复夹具保持 `f0fe53f2e0cf75fa7ad6e5020fb75cb67c282649` 和原 SHA256 清单，不更改网站、认证、配额、隐私依赖或生产开关。

## 已知证据

- iOS `87129f1`：push `35583144830` 失败；同 head PR `35583149129` 完整成功。不能以成功运行替代失败。
- 失败宿主 PID4458 已进入 authRestore/refreshRequest/heldPolling，随后两次 evidence 请求超时；最后提交快照仍为 generation0、heldfalse、requests0、operations0。服务日志停在 refresh/start。
- 成功运行 A11 的两次 refresh 分别耗时1647ms和23439ms，而后续A12/A13的refresh均在37–46ms。准备、密码和初始D1快照都正常。延迟集中于最初阶段，但现有数据不能确认是线程调度、磁盘同步、Node事件循环、内部HTTP还是Worker/D1。

## 诊断观察器

驱动通过可选 Node `--import` 加载观察器，不修改固定后端源码。只记录固定事件名、请求序号、固定路由类别、HTTP状态码、UTC毫秒、相对耗时、事件循环心跳延迟及累计CPU使用。Undici事件记录发送与收到响应头的时点；不记录URL、查询、headers、body、SQL、环境值或错误消息。

观察器透明调用原 `fsyncSync`，仅在超过100ms时记录耗时（无文件路径或fd），退出时恢复原函数。它不跳过同步，不添加写请求重试，不改变返回值或异常。记录文件0600，最多256KiB，心跳1秒且unref；HTTP轮询不新增磁盘fsync。Swift固定阶段和失败标记补UTC时点，原PID、边界、Keychain和120秒断言不变。

观察数据只用于诊断，不成为验收成功条件。真实HTTP脱敏/固定标签/预算/事件循环阻塞检测测试及原传输脱敏测试共4项通过；原宿主驱动20项通过。Xcode27 beta6本机真实A11/A12/A13通过（请求间隔0.579/0.523/0.517秒），观察器158条记录、15531字节、最大心跳延迟14ms、未观察到慢fsync。本机不代表远端或实体机验收。

## 远端复现与预算修正

诊断提交 `3961746` 的 push run [35609621182](https://github.com/xdgf558/station-cat-music-ios/actions/runs/35609621182) 失败。保留失败记录，未以另一条运行代替。脱敏产物显示：

| 事件 | UTC epoch 毫秒 |
| --- | ---: |
| App refreshRequest / heldPolling 开始 | 1789999827949 / 1789999827951 |
| Worker refresh 开始 / 成功返回 | 1789999828138 / 1789999839422 |
| App heldPolling 报 unavailable | 1789999838187 |
| D1 evidence_snapshot 开始 / 完成 | 1789999840335 / 1789999846410 |

Worker 刷新耗时11.284秒，随后快照耗时6.075秒。观察期间证据 GET 正常返回200和提交前快照；最终快照确认 generation1、未撤销、heldtrue、一次成功请求和一次操作。不是本次 evidence GET 阻塞：探针独立的10秒等待先于仍在既有35秒请求预算内的成功提交而失败。

同时记录到一次3331ms的fsync与22629ms的事件循环延迟；这是宿主停顿的证据，但没有足够数据进一步归因到具体磁盘、操作系统或调度原因。也不能用本次结果解释所有历史失败。

修复仅将 A11 CRASH 的首次提交观察预算与既有刷新请求35秒预算共用常量。刷新请求35秒、资源40秒、单次证据GET2秒、恢复后证据等待10秒及服务端120秒重放期限均不变。没有重试刷新或放宽 generation、PID、请求次数、操作数量、回执一致性断言；超时仍取消等待并失败。

新增11秒真实等待回归：旧实现10.172秒失败；修复后11.103秒通过。探针单元套件10项中8项通过、2项专用进程入口按设计跳过，包含绝对截止取消和不可重试错误测试；宿主驱动20项通过。修复后的本机真实A11/A12/A13全部通过，请求间隔0.473/0.537/0.526秒。使用Xcode27 beta6，不替代固定Xcode26.4.1远端验收。

网站仍固定 `f0fe53f`，产品修复保留，暂不合并。新提交需要重新取得push与PR的完整远端结果；实体机、真实HTTPS/AASA、完整销户和生产启用仍未验收。
