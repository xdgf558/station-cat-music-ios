# v1.0 收尾第一批：缓存与本机存储预算

日期：2026-09-20。目标是先上架不售卖订阅的 v1.0。基线为已合并的 iOS #7 / 网站 #177；本批不等于整个 R1 或 M5 发布验收完成。

## 实现

列表、歌曲详情和系统 Now Playing 现在复用同一个 ArtworkLoader。它在独立 actor 上完成下载、磁盘 I/O、ImageIO 解码；SwiftUI 不再使用未受此预算控制的 AsyncImage。请求只接受配置主机的 HTTPS，拒绝凭据 URL、非 443 端口与重定向，不发送 Cookie/认证信息。单张下载最多 2 MiB，资源超时五秒，像素尺寸最多 8192 × 8192，输出最大 512px 缩略图。

独立 PublicMusicArtwork-v1 缓存目录存放二进制 plist：仅图片数据、格式版本与有效期，文件名为完整 URL 的 SHA256（保留封面版本查询参数差异），不写原始 URL 或账号身份。缓存最多 150 MiB 编码文件内容、4096 项，按最近使用时间回收；只持久化服务端显式 public / max-age 的图片，尊重 Age、no-store、no-cache、private、Set-Cookie，并将本机复用期上限限制为一天。过期/损坏文件按未命中处理，不在失败时无限重试。目录排除备份、文件使用首次解锁保护。该目录是可重建的公开封面，系统仍可自主回收。

“清理临时缓存”接入共享缓存。代次校验阻止清理前启动的网络请求重新写回图片；不删除收藏、历史、凭据或未上传操作。缓存写入失败不阻断播放。已显示的图片可能继续保留在视图/系统媒体信息内，下一次加载按缓存规则处理。

PersonalMusic 保留每作用域 20 MiB 限制，增加全目录 100 MiB 编码内容和 64 个文件的预算；写入前检查、成功落盘后才发布内存状态。原子替换短暂需要额外一份单文件空间（最多 20 MiB）；这里不把逻辑内容字节数冒充文件系统实际分配块数。达到限制时拒绝增长并呈现原有存储错误，允许已有超限文件缩小。不自动驱逐账号文件，不把未同步收藏、隐私指令或游客资料当作缓存。账户切换/保存后释放可从磁盘恢复的非活跃内存副本。损坏/符号链接文件拒绝读写，原始文件保留。

## 业务规则已确认，尚未实施完整销户

用户选择公开评论匿名保留，其余按建议：个人资料继续清理，余额和争议另立处理记录，必要账务凭据最小化保留，不自动退款或清零。网站 policy-draft.json 记录此决定，整体 approved/executionEnabled 仍为 false。

现有 reader_comments.account_id 非空且 CASCADE，匿名保留需要额外迁移与读取适配；现有代码不能直接删账号。完整执行器、财务最小字段/期限、备份恢复屏障、服务端个人库墓碑与幂等记录长期压缩，以及多账号持久文件的显式回收流程仍未完成。此次增加的是预算与保留保护，不将限制写入描述为已完成长期回收。

## 验证与边界

本机 Xcode 27 beta 6 / iOS 27 模拟器，不是固定稳定 CI 或实体 iPhone。34 项首轮专项通过：5 项缓存、2 项封面处理与主线程心跳、4 项总量/文件数/隔离清理、23 项原个人库隐私依赖链。完整常规回归：126 项 Swift 用例，107 通过、19 项专用探针按设计跳过；UI 3/3 通过。之后补充独立字节预算用例，最终缓存专项 6/6 通过（其余产品源码未再变化）。Mock / Development / Staging / Production 四配置构建通过，源码守卫与 git diff --check 通过。网站销户盘点 11/11 与 153 页构建通过；构建使用 ALLOW_EMPTY_SERIAL_CONTENT=1，仅用于验证，不是生产发布包。网站只更新业务规则文档，无运行时变更。

测试覆盖缓存重开复用、版本 URL、不可缓存/无效响应、在途清理、LRU/TTL/损坏、符号链接隔离、总预算失败原始日志不变、存储超限仍可缩小、设置页清缓存保留历史 veto。网络使用本机 URLProtocol 合成图片，无真实账号。

未修改音频授权、认证、支付、生产开关或固定后端清单；未接生产数据、未部署、未上传 TestFlight。既有 M5 远端证据只代表其原提交，不能代替本批验证。

本机日志保存在未跟踪的 `evidence/v1-storage/`。新批次未推送、未执行远端 CI；需后续 PR 验证。
